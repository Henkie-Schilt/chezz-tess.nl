"use client";
import { atom } from "jotai";

export const categoryAtom = atom("Alles");
export const checkoutFormAtom = atom({});
export const shoppingCartAtom = atom([]);
export const contactFormAtom = atom({});
export const checkoutStepAtom = atom(0);
export const deliveryAtom = atom(true);
